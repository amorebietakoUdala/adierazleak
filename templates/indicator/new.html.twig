{% extends 'base.html.twig' %}

{% block title %}{{ 'title.indicator.new' | trans | capitalize }}{% endblock %}

{% block body %}
    <h3>{{ 'title.indicator.new' | trans | capitalize }}</h3>

    {{ include('indicator/_form.html.twig') }}

    <a href="{{ path('indicator_index') }}">back to list</a>

    {{ include('indicator/_delete_form.html.twig') }}
{% endblock %}
