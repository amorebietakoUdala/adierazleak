<template id="my-template">
	<swal-title>
		{{ 'message.confirmationTitle' | trans({}, 'alerts') }}
	</swal-title>
	<swal-icon type="question" color="grey"></swal-icon>
	<swal-html>{{'message.confirmationText' | trans({}, 'alerts') }}</swal-html>
	<swal-button type="confirm">
		{{ 'btn.accept' | trans({}, 'alerts') }}
	</swal-button>
	<swal-button type="cancel">
		{{ 'btn.cancel' | trans({}, 'alerts') }}
	</swal-button>
	<swal-param name="allowEscapeKey" value="false"/>
	<swal-param name="customClass" value='{ "popup": "my-popup" }'/>
</template>

<template id="error">
	<swal-title>
		{{ 'message.error' | trans({}, 'alerts') }}
	</swal-title>
	<swal-icon type="error" color="red"></swal-icon>
	<swal-html>Loading...</swal-html>
	<swal-button type="confirm">
		{{ 'btn.ok' | trans({}, 'alerts') }}
	</swal-button>
	<swal-param name="allowEscapeKey" value="true"/>
</template>
